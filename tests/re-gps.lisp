(defpackage re-gps-test
  (:use :cl
        :re-gps
        :prove))
(in-package :re-gps-test)

;; NOTE: To run this test file, execute `(asdf:test-system :re-gps)' in your Lisp.

(plan nil)

;; blah blah blah.

(finalize)
